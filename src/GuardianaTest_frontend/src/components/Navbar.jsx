import React from 'react';
import './Navbar.scss'; // Cambiamos la extensión a .scss

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="navbar-brand">
        <h1>Seguridad Femenina</h1>
      </div>
    </nav>
  );
};

export default Navbar;
